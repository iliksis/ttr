// Dev-config Server URL and Ingestion key. #17 replaces the hardcoded key
// with an options page backed by chrome.storage.local; the Server URL stays
// a build-time constant.
export const SERVER_URL = "http://localhost:8080";
export const INGESTION_KEY = "dev-ingestion-key";
